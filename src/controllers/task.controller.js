// import { getAll, getById, create, update, remove } from '../models/task.model.js'

// getTasks   - GET /api/tasks        → lista todas las tareas
// getTask    - GET /api/tasks/:id    → retorna una tarea por id
// createTask - POST /api/tasks       → crea una nueva tarea
// updateTask - PUT /api/tasks/:id    → actualiza una tarea por id
// deleteTask - DELETE /api/tasks/:id → elimina una tarea por id
